
/**
 * This is another class not in the public API.
 */
export class OtherClass {

    constructor() {
        const source = require('../shaders/screenaligned.vert');
        console.log(`source: ${source}`);
    }
}
